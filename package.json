{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "test:api": "pnpm --filter @workspace/api-server run test",
    "test:frontend": "pnpm --filter @workspace/nexus-digital run test",
    "test:ci": "pnpm run test:api && pnpm run test:frontend",
    "test:bdd": "cucumber-js -p . --format progress --require-module module.exports=cucumber.js",
    "test:integration": "pnpm --filter @workspace/api-server run test:src/__tests__/integration.test.ts"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "esbuild": "0.27.7"
  }
}
